import styled from 'styled-components';
import RestaurantComponent from './RestaurantComponent';

const StyledRestaurantCard = styled(RestaurantComponent)`
  padding: 1em;
  border: 1px solid #C7C4C0;
  border-radius: 3px;
  margin: 1em 0;
`;

export default StyledRestaurantCard;
